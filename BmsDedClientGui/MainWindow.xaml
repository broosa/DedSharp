<Window x:Class="DedSharp.BmsDedClientGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DedSharp.BmsDedClientGui"
        mc:Ignorable="d"
        Title="BMS DED Client"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        SizeToContent="WidthAndHeight"
        Name="DedMainWindow"> 
    <Window.Resources>
        <Style x:Key="ConnectionLabelStyle" TargetType="Label">
            <Setter Property="FontWeight"  Value="Bold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Content,RelativeSource={RelativeSource Self}}" Value="CONNECTED">
                    <Setter Property="Foreground" Value="Green"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Content,RelativeSource={RelativeSource Self}}" Value="DISCONNECTED">
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <local:ConnectionBooleanLabelConverter x:Key="ConnectionBooleanLabelConverter" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0"  Content="BMS Connection Status:"/>
        <Label Grid.Row="0" Grid.Column="1" Name="BmsConnectionStatusLabel" HorizontalAlignment="Right" Content="{Binding Path=IsBmsConnected, Converter={StaticResource ConnectionBooleanLabelConverter}}" Style="{StaticResource ConnectionLabelStyle}"/>
        <Label Grid.Row="1" Grid.Column="0" HorizontalAlignment="Stretch" Content="DED Device Status:"/>
        <Label Grid.Row="1" Grid.Column="1" Name="DedConnectionStatusLabel" HorizontalAlignment="Right" Content="{Binding Path=IsDedConnected, Converter={StaticResource ConnectionBooleanLabelConverter}}" Style="{StaticResource ConnectionLabelStyle}"/>
        <local:DedImageMirror x:Name="DedMirror" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" DedDisplayProvider="{Binding Path=DedDisplayProvider, ElementName=DedMainWindow}"/>
    </Grid>
</Window>
